import { CheckloginDirective } from './checklogin.directive';

describe('CheckloginDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckloginDirective();
    expect(directive).toBeTruthy();
  });
});
